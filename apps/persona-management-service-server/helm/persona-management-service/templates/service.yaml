apiVersion: v1
kind: Service
metadata:
  name: {{ include "persona-management-service.fullname" . }}
  labels:
    {{- include "persona-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "persona-management-service.selectorLabels" . | nindent 4 }}
